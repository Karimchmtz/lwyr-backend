spring:
  datasource:
    url: jdbc:tc:postgresql:17-alpine:///lwyr_db
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

logging:
  level:
    org.springframework.security: WARN
    com.lwyr: DEBUG
